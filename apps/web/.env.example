# Browser requests stay on the Next.js origin.
NEXT_PUBLIC_API_BASE_URL=/api/backend

# Server-only destination used by the auth proxy Route Handler.
# Use http://localhost:3000 when running services/api locally.
SCILAB_API_BASE_URL=https://scilab-api.epsilon.io.vn
